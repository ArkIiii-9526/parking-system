# 智慧停车系统 - 后端接口补充需求文档

> 本文档基于前端现有功能模块与后端API文档对比分析，整理出需要后端补充或增强的接口需求，助力后端开发完善系统功能。

---

## 一、分析概述

### 1.1 已完整对接的模块

| 模块名称 | 状态 | 说明 |
|---------|------|------|
| 认证管理 | ✅ 完整 | 登录/登出/刷新Token |
| 停车场管理 | ✅ 完整 | 分页查询/CRUD/附近搜索/统计 |
| 车位分区管理 | ✅ 完整 | 分页查询/CRUD/统计 |
| 停车位管理 | ✅ 完整 | 分页查询/CRUD/状态更新/预约释放 |
| 模拟数据管理 | ✅ 完整 | 生成/更新/重置/清除 |
| 计费规则管理 | ✅ 完整 | 分页查询/CRUD/启用禁用/试算 |
| 数据统计分析 | ✅ 完整 | 周转率/趋势/收入/利用率/汇总/多种导出 |
| 停车引导 | ✅ 完整 | 推荐/导航/路径规划 |
| 预约管理 | ✅ 完整 | 分页查询/CRUD/取消/用户预约/停车场预约 |
| 系统用户管理 | ✅ 完整 | 列表/CRUD/角色分配/权限查询/菜单获取 |
| 系统角色管理 | ✅ 完整 | 列表/CRUD/权限绑定/继承 |
| 系统权限管理 | ✅ 完整 | 列表/树/CRUD/刷新缓存 |
| 系统配置管理 | ✅ 完整 | 分页查询/CRUD/按类型获取/缓存刷新 |
| 审计日志管理 | ✅ 完整 | 列表/详情/清空 |
| 缓存监控管理 | ✅ 完整 | 指标/重置/按模式清除/全部清除 |

### 1.2 需要补充或增强的模块

| 模块名称 | 优先级 | 说明 |
|---------|--------|------|
| 车辆进出管理 | 🔴 高 | 缺少全局分页查询、导出功能 |
| Dashboard数据 | 🔴 高 | 建议提供聚合接口 |
| 收费记录管理 | 🟡 中 | 缺少详情接口、周/月报表 |
| 预约管理增强 | 🟡 中 | 缺少统计分析、导出功能 |
| 审计日志增强 | 🟡 中 | 建议增加分页、导出功能 |
| 通用功能 | 🟢 低 | 数据字典、消息通知、批量操作 |

---

## 二、高优先级接口需求

### 2.1 车辆进出管理模块补充

#### 2.1.1 分页查询所有车辆进出记录

**背景**：当前只能按单个停车场(`/vehicle/records/parking/{parkingId}`)或按车牌号(`/vehicle/records/car/{carNo}`)查询，前端需要一个全局的分页查询接口来展示所有停车场的进出记录。

**建议接口**：
```
GET /api/vehicle/records/page
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| pageNo | Integer | 查询参数 | 页码（默认1） | 否 |
| pageSize | Integer | 查询参数 | 每页条数（默认10） | 否 |
| parkingId | Long | 查询参数 | 停车场ID（可选） | 否 |
| carNo | String | 查询参数 | 车牌号（模糊查询） | 否 |
| status | Integer | 查询参数 | 状态：0在场，1已出场 | 否 |
| startTime | String | 查询参数 | 入场开始时间 | 否 |
| endTime | String | 查询参数 | 入场结束时间 | 否 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "carNo": "京A12345",
        "parkingId": 1,
        "parkingName": "智慧停车场",
        "spaceId": 1,
        "spaceCode": "A-001",
        "entryTime": "2024-01-15T10:00:00",
        "exitTime": "2024-01-15T12:00:00",
        "status": 1,
        "fee": 10.00
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1
  }
}
```

**权限要求**：`billing:view`

---

#### 2.1.2 导出车辆进出记录

**背景**：前端有导出需求，但后端暂无对应接口。

**建议接口**：
```
POST /api/vehicle/records/export
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| parkingId | Long | 请求体 | 停车场ID | 否 |
| carNo | String | 请求体 | 车牌号 | 否 |
| status | Integer | 请求体 | 状态 | 否 |
| startTime | String | 请求体 | 开始时间 | 否 |
| endTime | String | 请求体 | 结束时间 | 否 |
| fileName | String | 请求体 | 自定义文件名 | 否 |

**响应结构**：文件流（Excel格式）

**权限要求**：`billing:export`

---

### 2.2 Dashboard聚合数据接口

**背景**：前端Dashboard页面需要展示多维度数据，目前需要调用多个API组合数据，建议后端提供一个聚合接口以提升性能和用户体验。

**建议接口**：
```
GET /api/dashboard/overview
```

**请求参数**：无

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "statistics": {
      "totalParkings": 5,
      "totalSpaces": 500,
      "availableSpaces": 200,
      "occupiedSpaces": 280,
      "reservedSpaces": 20,
      "usageRate": 60.0,
      "todayEntries": 150,
      "todayExits": 130,
      "activeVehicles": 280
    },
    "todayRevenue": {
      "totalAmount": 5000.00,
      "transactionCount": 130,
      "averageAmount": 38.46
    },
    "recentRecords": [
      {
        "id": 1,
        "carNo": "京A12345",
        "parkingId": 1,
        "parkingName": "智慧停车场",
        "entryTime": "2024-01-15T10:00:00",
        "exitTime": null,
        "status": 0,
        "fee": null
      }
    ],
    "parkingOverview": [
      {
        "parkingId": 1,
        "parkingName": "智慧停车场",
        "totalSpaces": 100,
        "availableSpaces": 40,
        "usageRate": 60.0
      }
    ],
    "updateTime": "2024-01-15T10:30:00"
  }
}
```

**权限要求**：无（基础数据查看）

---

## 三、中优先级接口需求

### 3.1 收费记录管理增强

#### 3.1.1 获取收费记录详情

**背景**：前端有详情展示需求，目前从列表数据获取，建议提供专门的详情接口。

**建议接口**：
```
GET /api/billing/records/{id}
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| id | Long | 路径参数 | 记录ID | 是 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "carNo": "京A12345",
    "parkingId": 1,
    "parkingName": "智慧停车场",
    "spaceId": 1,
    "spaceCode": "A-001",
    "entryTime": "2024-01-15T10:00:00",
    "exitTime": "2024-01-15T12:00:00",
    "duration": 120,
    "totalAmount": 10.00,
    "discountAmount": 0.00,
    "actualAmount": 10.00,
    "status": 1,
    "paymentMethod": "wechat",
    "paymentTime": "2024-01-15T12:00:30",
    "transactionNo": "WX202401151200001",
    "ruleId": 1,
    "ruleName": "标准计费",
    "feeDetails": [
      {
        "period": "首小时",
        "time": 60,
        "fee": 5.00
      },
      {
        "period": "超时",
        "time": 60,
        "fee": 5.00
      }
    ]
  }
}
```

**权限要求**：`billing:view`

---

#### 3.1.2 周报表统计

**建议接口**：
```
GET /api/billing/records/statistics/weekly
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| parkingId | Long | 查询参数 | 停车场ID | 否 |
| weekStart | String | 查询参数 | 周开始日期（格式：yyyy-MM-dd） | 否，默认本周 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "weekStart": "2024-01-15",
    "weekEnd": "2024-01-21",
    "totalAmount": 35000.00,
    "totalCount": 900,
    "averageDailyAmount": 5000.00,
    "dailyList": [
      {
        "date": "2024-01-15",
        "dayOfWeek": "周一",
        "amount": 5000.00,
        "count": 130
      }
    ],
    "comparedToLastWeek": {
      "amountChange": 2000.00,
      "amountChangeRate": 6.06,
      "countChange": 50,
      "countChangeRate": 5.88
    }
  }
}
```

**权限要求**：`billing:statistics`

---

#### 3.1.3 月报表统计

**建议接口**：
```
GET /api/billing/records/statistics/monthly
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| parkingId | Long | 查询参数 | 停车场ID | 否 |
| month | String | 查询参数 | 月份（格式：yyyy-MM） | 否，默认本月 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "month": "2024-01",
    "totalAmount": 150000.00,
    "totalCount": 3900,
    "averageDailyAmount": 5000.00,
    "peakDay": {
      "date": "2024-01-20",
      "amount": 8000.00,
      "count": 200
    },
    "weeklyList": [
      {
        "weekNumber": 1,
        "weekStart": "2024-01-01",
        "weekEnd": "2024-01-07",
        "amount": 35000.00,
        "count": 900
      }
    ],
    "comparedToLastMonth": {
      "amountChange": 10000.00,
      "amountChangeRate": 7.14,
      "countChange": 200,
      "countChangeRate": 5.41
    }
  }
}
```

**权限要求**：`billing:statistics`

---

### 3.2 预约管理增强

#### 3.2.1 预约统计分析

**背景**：前端缺少预约数据的统计分析功能。

**建议接口**：
```
GET /api/reservations/statistics
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| parkingId | Long | 查询参数 | 停车场ID | 否 |
| startDate | String | 查询参数 | 开始日期 | 否 |
| endDate | String | 查询参数 | 结束日期 | 否 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "totalReservations": 500,
    "completedCount": 400,
    "cancelledCount": 50,
    "expiredCount": 30,
    "pendingCount": 20,
    "completionRate": 80.0,
    "cancellationRate": 10.0,
    "averageReservationDuration": 180,
    "peakHours": [
      {
        "hour": 9,
        "count": 80
      },
      {
        "hour": 18,
        "count": 75
      }
    ],
    "dailyTrend": [
      {
        "date": "2024-01-15",
        "count": 25
      }
    ],
    "analysisPeriod": "2024-01-01 至 2024-01-31"
  }
}
```

**权限要求**：`reservation:view`

---

#### 3.2.2 导出预约记录

**建议接口**：
```
POST /api/reservations/export
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| parkingId | Long | 请求体 | 停车场ID | 否 |
| carNo | String | 请求体 | 车牌号 | 否 |
| status | Integer | 请求体 | 状态 | 否 |
| startDate | String | 请求体 | 开始日期 | 否 |
| endDate | String | 请求体 | 结束日期 | 否 |
| fileName | String | 请求体 | 自定义文件名 | 否 |

**响应结构**：文件流（Excel格式）

**权限要求**：`reservation:export`

---

#### 3.2.3 批量取消预约

**建议接口**：
```
PUT /api/reservations/batch/cancel
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| ids | List<Long> | 请求体 | 预约ID列表 | 是 |
| reason | String | 请求体 | 取消原因 | 否 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "successCount": 8,
    "failCount": 2,
    "failedIds": [5, 9],
    "failReasons": {
      "5": "预约已完成，无法取消",
      "9": "预约已取消"
    }
  }
}
```

**权限要求**：`reservation:cancel`

---

### 3.3 审计日志管理增强

#### 3.3.1 分页查询审计日志

**背景**：当前 `/api/audit-log/list` 返回列表数据，数据量大时可能存在性能问题。

**建议接口**：
```
GET /api/audit-log/page
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| pageNo | Integer | 查询参数 | 页码（默认1） | 否 |
| pageSize | Integer | 查询参数 | 每页条数（默认20） | 否 |
| username | String | 查询参数 | 操作用户名 | 否 |
| businessType | String | 查询参数 | 业务类型 | 否 |
| module | String | 查询参数 | 模块名称 | 否 |
| status | Integer | 查询参数 | 状态：0失败，1成功 | 否 |
| startTime | String | 查询参数 | 开始时间 | 否 |
| endTime | String | 查询参数 | 结束时间 | 否 |

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [...],
    "total": 1000,
    "size": 20,
    "current": 1
  }
}
```

**权限要求**：`sys:audit:query`

---

#### 3.3.2 导出审计日志

**建议接口**：
```
POST /api/audit-log/export
```

**请求参数**：
| 参数名 | 类型 | 位置 | 描述 | 必填 |
|-------|------|------|------|------|
| username | String | 请求体 | 操作用户名 | 否 |
| businessType | String | 请求体 | 业务类型 | 否 |
| startTime | String | 请求体 | 开始时间 | 否 |
| endTime | String | 请求体 | 结束时间 | 否 |
| fileName | String | 请求体 | 自定义文件名 | 否 |

**响应结构**：文件流（Excel格式）

**权限要求**：`sys:audit:export`

---

#### 3.3.3 获取审计日志业务类型列表

**背景**：前端筛选业务类型时需要获取系统中所有业务类型。

**建议接口**：
```
GET /api/audit-log/business-types
```

**请求参数**：无

**响应结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "code": "LOGIN",
      "name": "登录"
    },
    {
      "code": "LOGOUT",
      "name": "登出"
    },
    {
      "code": "VEHICLE_ENTRY",
      "name": "车辆入场"
    },
    {
      "code": "VEHICLE_EXIT",
      "name": "车辆出场"
    },
    {
      "code": "BILLING_PAY",
      "name": "收费支付"
    }
  ]
}
```

**权限要求**：`sys:audit:query`

---

## 四、低优先级接口需求

### 4.1 数据字典管理

**背景**：系统中有多处状态码、类型码（如车位状态、支付状态、预约状态、车位类型等），建议统一管理。

#### 4.1.1 获取字典列表

**建议接口**：
```
GET /api/sys/dict/list
```

#### 4.1.2 获取字典详情

**建议接口**：
```
GET /api/sys/dict/{dictType}
```

#### 4.1.3 字典数据CRUD

**建议接口**：
```
POST /api/sys/dict
PUT /api/sys/dict
DELETE /api/sys/dict/{dictId}
```

**建议的字典类型**：
| 字典类型 | 说明 | 字典项示例 |
|---------|------|----------|
| parking_space_status | 车位状态 | 1-空闲, 2-占用, 3-已预约 |
| payment_status | 支付状态 | 0-未支付, 1-已支付, 2-部分支付 |
| reservation_status | 预约状态 | 0-待使用, 1-使用中, 2-已完成, 3-已取消, 4-已过期 |
| space_type | 车位类型 | 1-普通, 2-VIP, 3-无障碍 |
| payment_method | 支付方式 | wechat-微信, alipay-支付宝, cash-现金, card-银行卡 |
| audit_business_type | 审计业务类型 | LOGIN, LOGOUT, VEHICLE_ENTRY 等 |

---

### 4.2 系统消息通知

**背景**：系统运营过程中需要消息通知功能（如预约提醒、费用提醒等）。

#### 4.2.1 发送系统消息

**建议接口**：
```
POST /api/sys/message/send
```

#### 4.2.2 获取用户消息

**建议接口**：
```
GET /api/sys/message/user/{userId}
```

#### 4.2.3 标记消息已读

**建议接口**：
```
PUT /api/sys/message/{id}/read
```

#### 4.2.4 获取未读消息数

**建议接口**：
```
GET /api/sys/message/unread-count
```

---

### 4.3 批量操作接口

**背景**：部分模块可能需要批量操作功能。

#### 4.3.1 批量删除停车场

**建议接口**：
```
DELETE /api/parkings/batch
```

**请求参数**：
```json
{
  "ids": [1, 2, 3]
}
```

#### 4.3.2 批量删除停车位

**建议接口**：
```
DELETE /api/parking-spaces/batch
```

#### 4.3.3 批量更新车位状态

**建议接口**：
```
PUT /api/parking-spaces/batch/status
```

**请求参数**：
```json
{
  "ids": [1, 2, 3],
  "status": 1
}
```

---

## 五、现有接口优化建议

### 5.1 接口响应优化

| 接口 | 现状 | 建议 |
|-----|------|------|
| `/api/audit-log/list` | 返回全部数据 | 增加默认分页或限制返回条数 |
| `/api/vehicle/records/parking/{parkingId}` | 无分页参数说明 | 建议返回分页数据 |

### 5.2 字段补充建议

| 接口 | 建议补充字段 |
|-----|-------------|
| 车辆进出记录 | `parkingName`（停车场名称）、`spaceCode`（车位编号） |
| 收费记录 | `ruleName`（计费规则名称）、`feeDetails`（费用明细） |
| 运营汇总 | `incomeTrend`（收入趋势）、`entryTrend`（入场趋势）、`exitTrend`（出场趋势）、`parkingStats`（各停车场统计） |

### 5.3 错误码规范建议

建议后端统一定义业务错误码：

| 错误码 | 说明 |
|-------|------|
| 10001 | 停车场不存在 |
| 10002 | 停车位不存在 |
| 10003 | 车位已被占用 |
| 10004 | 车位已被预约 |
| 20001 | 车辆未入场 |
| 20002 | 车辆已出场 |
| 20003 | 存在未支付账单 |
| 30001 | 预约不存在 |
| 30002 | 预约已取消 |
| 30003 | 预约已过期 |
| 30004 | 预约时间冲突 |
| 40001 | 计费规则不存在 |
| 40002 | 计费规则已禁用 |

---

## 六、接口权限码汇总

以下是建议新增接口对应的权限码：

| 权限码 | 说明 |
|-------|------|
| `billing:export` | 导出收费记录 |
| `reservation:export` | 导出预约记录 |
| `reservation:cancel` | 取消预约（批量） |
| `sys:audit:export` | 导出审计日志 |
| `sys:dict:view` | 查看数据字典 |
| `sys:dict:add` | 新增数据字典 |
| `sys:dict:edit` | 编辑数据字典 |
| `sys:dict:delete` | 删除数据字典 |
| `sys:message:view` | 查看系统消息 |
| `sys:message:send` | 发送系统消息 |

---

## 七、实施建议

### 7.1 优先级排序

1. **第一阶段（高优先级）**
   - 车辆进出记录分页查询
   - Dashboard聚合数据接口
   
2. **第二阶段（中优先级）**
   - 收费记录详情接口
   - 周/月报表统计
   - 预约统计分析
   - 审计日志分页查询
   
3. **第三阶段（低优先级）**
   - 数据字典管理
   - 系统消息通知
   - 批量操作接口

### 7.2 前端配合事项

- 高优先级接口完成后，前端将同步更新相关页面
- 建议后端完成接口后及时更新API文档
- 新增权限码后需要同步更新权限配置数据

---

## 八、版本记录

| 版本 | 日期 | 说明 |
|-----|------|------|
| v1.0 | 2026-03-25 | 初始版本，基于前端代码与后端API文档分析 |

---

> 文档作者：前端开发团队  
> 最后更新：2026-03-25
